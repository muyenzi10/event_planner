<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Booking | Event Planner Pro</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <style>
    :root {
      --primary: #6a11cb;
      --secondary: #4361ee;
      --accent: #f72585;
      --light: #f8f9fa;
      --dark: #212529;
      --success: #4cc9f0;
    }

    body {
      background: linear-gradient(135deg, #f5f7fa, #e4e7f1);
      min-height: 100vh;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }

    /* Animations */
    @keyframes fadeInDown {
      from { opacity: 0; transform: translateY(-10px); }
      to { opacity: 1; transform: translateY(0); }
    }

    @keyframes scaleFadeIn {
      0% { transform: scale(0.95); opacity: 0; }
      100% { transform: scale(1); opacity: 1; }
    }

    @keyframes fadeInUp {
      0% { transform: translateY(20px); opacity: 0; }
      100% { transform: translateY(0); opacity: 1; }
    }

    /* Card styling */
    .booking-card {
      background: white;
      border-radius: 18px;
      box-shadow: 0 12px 40px rgba(0, 0, 0, 0.08);
      overflow: hidden;
      transition: all 0.3s ease-in-out;
      animation: scaleFadeIn 0.7s ease forwards;
    }

    .booking-card:hover {
      transform: translateY(-5px);
      box-shadow: 0 18px 50px rgba(0, 0, 0, 0.12);
    }

    /* Header */
    .form-header {
      background: linear-gradient(270deg, var(--primary), var(--secondary));
      background-size: 400% 400%;
      animation: fadeInDown 0.6s ease, gradientShift 8s ease infinite;
      color: white;
      padding: 30px;
      border-radius: 18px 18px 0 0;
    }

    @keyframes gradientShift {
      0% { background-position: 0% 50%; }
      50% { background-position: 100% 50%; }
      100% { background-position: 0% 50%; }
    }

    /* Section styling */
    .form-section {
      padding: 30px;
    }

    .section-title {
      position: relative;
      padding-bottom: 10px;
      margin-bottom: 25px;
      font-weight: 600;
      color: var(--dark);
      animation: fadeInUp 0.6s ease both;
    }

    .section-title:after {
      content: '';
      position: absolute;
      bottom: 0;
      left: 0;
      width: 50px;
      height: 3px;
      background: linear-gradient(to right, var(--primary), var(--secondary));
      border-radius: 3px;
    }

    /* Inputs */
    .form-control:focus,
    .form-select:focus {
      border-color: var(--secondary);
      box-shadow: 0 0 0 0.25rem rgba(67, 97, 238, 0.25);
    }

    input, textarea, select {
      animation: fadeInUp 0.6s ease both;
    }

    .form-label {
      font-weight: 600;
      color: #495057;
      margin-bottom: 8px;
      display: flex;
      align-items: center;
    }

    .form-label i {
      margin-right: 10px;
      color: var(--secondary);
      width: 20px;
    }

    /* Buttons with ripple effect - UPDATED FOR MOBILE */
    .btn-primary,
    .btn-outline-secondary {
      position: relative;
      overflow: hidden;
      padding: 10px 20px; /* Reduced padding for better mobile fit */
      font-weight: 600;
      border-radius: 8px;
      font-size: 1rem; /* Adjusted font size */
    }

    @media (min-width: 768px) {
      .btn-primary,
      .btn-outline-secondary {
        padding: 12px 26px; /* Original padding for larger screens */
        font-size: 1.05rem; /* Original font size for larger screens */
      }
    }

    .btn-primary {
      background: linear-gradient(to right, var(--primary), var(--secondary));
      border: none;
      transition: all 0.3s ease;
    }

    .btn-primary:hover {
      background: linear-gradient(to right, #5a0db9, #3250d8);
      transform: translateY(-2px);
    }

    .btn-primary::after,
    .btn-outline-secondary::after {
      content: "";
      position: absolute;
      left: 50%;
      top: 50%;
      transform: translate(-50%, -50%) scale(0);
      width: 300%;
      height: 300%;
      background: rgba(255,255,255,0.2);
      border-radius: 50%;
      transition: transform 0.5s ease;
    }

    .btn-primary:hover::after,
    .btn-outline-secondary:hover::after {
      transform: translate(-50%, -50%) scale(1);
    }

    .required-star {
      color: var(--accent);
      margin-left: 3px;
    }

    /* Button container - UPDATED FOR MOBILE */
    .form-actions {
      display: flex;
      flex-direction: column; /* Stack buttons vertically on mobile */
      gap: 15px; /* Space between buttons */
      align-items: stretch;
      margin-top: 30px;
      padding-top: 25px;
      border-top: 1px solid #eee;
    }

    @media (min-width: 768px) {
      .form-actions {
        flex-direction: row; /* Side by side on larger screens */
        justify-content: space-between;
        align-items: center;
      }
    }

    @media (max-width: 768px) {
      .booking-card { margin-top: 20px; }
      .form-header { text-align: center; padding: 20px; }
      .form-section { padding: 20px; }
    }
  </style>
</head>
<body>
  <div class="container py-4"> <!-- Reduced padding for mobile -->
    <div class="row justify-content-center">
      <div class="col-lg-10">
        <div class="booking-card">
          <div class="form-header">
            <h2 class="mb-0"><i class="fas fa-calendar-edit me-2"></i>Edit Booking</h2>
            <p class="mb-0 mt-1">Update your wedding details below</p>
          </div>

          <div class="form-section">
            <form action="/Dashboard/booking/update/<%= booking._id %>?_method=PUT" method="POST">
              <div class="row">
                <div class="col-md-6" style="animation: fadeInUp 0.6s ease both; animation-delay: 0.2s;">
                  <h5 class="section-title">Personal Information</h5>

                  <div class="mb-4">
                    <label class="form-label"><i class="fas fa-user"></i>First Name<span class="required-star">*</span></label>
                    <input type="text" name="firstname" class="form-control form-control-lg" value="<%= booking.firstname %>" required>
                  </div>

                  <div class="mb-4">
                    <label class="form-label"><i class="fas fa-user"></i>Last Name<span class="required-star">*</span></label>
                    <input type="text" name="lastname" class="form-control form-control-lg" value="<%= booking.lastname %>" required>
                  </div>

                  <div class="mb-4">
                    <label class="form-label"><i class="fas fa-envelope"></i>Email Address<span class="required-star">*</span></label>
                    <input type="email" name="email" class="form-control form-control-lg" value="<%= booking.email %>" required>
                  </div>

                  <div class="mb-4">
                    <label class="form-label"><i class="fas fa-phone"></i>Phone Number<span class="required-star">*</span></label>
                    <input type="text" name="phone" class="form-control form-control-lg" value="<%= booking.phone %>" required>
                  </div>
                </div>

                <div class="col-md-6" style="animation: fadeInUp 0.6s ease both; animation-delay: 0.4s;">
                  <h5 class="section-title">Wedding Details</h5>

                  <div class="mb-4">
                    <label class="form-label"><i class="fas fa-calendar-day"></i>Wedding Date<span class="required-star">*</span></label>
                    <input type="date" name="weddingDate" class="form-control form-control-lg" value="<%= booking.weddingDate.toISOString().slice(0,10) %>" required>
                  </div>

                  <div class="mb-4">
                    <label class="form-label"><i class="fas fa-users"></i>Number of Guests<span class="required-star">*</span></label>
                    <input type="number" name="guestsNumber" class="form-control form-control-lg" value="<%= booking.guestsNumber %>" min="1" required>
                  </div>

                  <div class="mb-4">
                    <label class="form-label"><i class="fas fa-ring"></i>Wedding Type<span class="required-star">*</span></label>
                    <input type="text" name="weddingType" class="form-control form-control-lg" value="<%= booking.weddingType %>" required>
                  </div>

                  <div class="mb-4">
                    <label class="form-label"><i class="fas fa-map-marker-alt"></i>Wedding Location<span class="required-star">*</span></label>
                    <input type="text" name="weddingLocation" class="form-control form-control-lg" value="<%= booking.weddingLocation %>" required>
                  </div>
                </div>
              </div>

              <h5 class="section-title" style="animation-delay: 0.6s;">Vision & Preferences</h5>
              <div class="mb-4" style="animation: fadeInUp 0.6s ease both; animation-delay: 0.7s;">
                <label class="form-label"><i class="fas fa-palette"></i>Wedding Vision & Theme</label>
                <textarea name="weddingVision" class="form-control form-control-lg" rows="5" placeholder="Describe your dream wedding..."><%= booking.weddingVision %></textarea>
              </div>

              <!-- UPDATED BUTTON SECTION FOR MOBILE -->
              <div class="form-actions" style="animation: fadeInUp 0.6s ease both; animation-delay: 0.9s;">
                <a href="/Dashboard/booking" class="btn btn-outline-secondary">
                  <i class="fas fa-times me-2"></i> Go back
                </a>
                <button type="submit" class="btn btn-primary">
                  <i class="fas fa-save me-2"></i>Update Booking
                </button>
              </div>
            </form>
          </div>
        </div>

        <div class="mt-4 text-center text-muted">
          <p class="mb-0">© 2023 Event Planner Pro | Your Dream Wedding Starts Here</p>
        </div>
      </div>
    </div>
  </div>
</body>
</html>