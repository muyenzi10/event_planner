<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>

  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">

  <style>
    body {
      background-color: #f8f9fa;
      display: flex;
      justify-content: center;
      align-items: center;
      min-height: 100vh;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }

    .message-box {
      background-color: #fff;
      border-radius: 12px;
      box-shadow: 0 8px 20px rgba(0,0,0,0.15);
      padding: 40px;
      max-width: 500px;
      text-align: center;
    }

    .message-box h2 {
      color: <%= error ? '#dc3545' : '#28a745' %>;
      margin-bottom: 20px;
    }

    .message-box p {
      font-size: 16px;
      margin-bottom: 10px;
    }

    .message-box .text-muted {
      font-size: 14px;
      color: #6c757d;
    }

    .message-box .btn-primary {
      padding: 10px 30px;
      border-radius: 8px;
      font-weight: bold;
      margin-top: 20px;
    }

    .message-icon {
      font-size: 50px;
      color: <%= error ? '#dc3545' : '#28a745' %>;
      margin-bottom: 15px;
    }
  </style>
</head>
<body>
  <div class="message-box">
    <!-- Checkmark for success, cross for error -->
    <div class="message-icon">
      <%- error ? '&#10060;' : '&#10004;' %>
    </div>

    <h2><%= title %></h2>
    <p><%= message %></p>

    <% if (!error) { %>
      <p><%= subMessage %></p>
      <p class="text-muted"><%= nextSteps %></p>
    <% } else { %>
      <p class="text-muted">Error details: <%= errorMessage %></p>
    <% } %>

    <a href="/" class="btn btn-primary">Back to Home</a>
  </div>

  <!-- Optional: auto-redirect after 5 seconds -->
  <script>
    setTimeout(() => {
      window.location.href = "/";
    }, 15000);
  </script>

  <!-- Bootstrap JS (optional) -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
