<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Wedding Planning | Dream Day Events</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;500;600;700&family=Montserrat:wght@300;400;500;600&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/allpack/layout/css/navbas.css">
    <link rel="stylesheet" href="/allpack/layout/css/footer.css">
    <style>
        /* =========================================== */
        /* BOOKING PAGE STYLES - NO CONFLICTS WITH FOOTER */
        /* =========================================== */
        
        /* Main booking page wrapper - keeps our styles contained */
        .booking-page-content {
            background-color: #f8f4f0;
            min-height: calc(100vh - 140px); /* Adjust based on navbar/footer height */
        }

        /* Hero Section - completely isolated */
        .booking-hero-section {
            position: relative;
            height: 80vh;
            overflow: hidden;
        }

        .booking-hero-video {
            background: #333;
            width: 100%;
            height: 100%;
            object-fit: cover;
            position: absolute;
            top: 0;
            left: 0;
        }

        .booking-hero-overlay {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.3);
            z-index: 1;
        }

        .booking-hero-content {
            position: relative;
            z-index: 2;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            height: 100%;
            color: white;
            text-align: center;
            padding: 0 20px;
        }

        .booking-hero-title {
            font-family: 'Playfair Display', serif;
            font-size: 4rem;
            margin-bottom: 20px;
            text-shadow: 2px 2px 8px rgba(0, 0, 0, 0.6);
            color: white;
            position: relative;
            display: inline-block;
            background: rgba(0, 0, 0, 0.3);
            padding: 15px 30px;
            border-radius: 10px;
        }
        
        .booking-hero-title::after {
            content: '';
            position: absolute;
            bottom: -10px;
            left: 50%;
            transform: translateX(-50%);
            width: 60%;
            height: 3px;
            background: rgba(255, 255, 255, 0.5);
        }

        .booking-hero-text {
            font-family: 'Montserrat', sans-serif;
            font-size: 1.5rem;
            max-width: 800px;
            margin-bottom: 30px;
            color: white;
            font-weight: 400;
            letter-spacing: 0.5px;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.5);
            background: rgba(0, 0, 0, 0.2);
            padding: 10px 20px;
            border-radius: 8px;
            backdrop-filter: blur(2px);
        }

        /* Booking Form Container */
        .booking-form-container {
            max-width: 1200px;
            margin: -100px auto 50px;
            padding: 0 20px;
            position: relative;
            z-index: 10;
        }

        .booking-form-card {
            border-radius: 15px;
            box-shadow: 0 15px 40px rgba(0, 0, 0, 0.1);
            overflow: hidden;
            background: #ffffff;
            border: 1px solid rgba(212, 175, 55, 0.2);
            transition: transform 0.3s ease;
        }

        .booking-form-card:hover {
            transform: translateY(-5px);
        }

        .booking-form-header {
            background: linear-gradient(135deg, #d4af37, #f1d27a);
            color: white;
            padding: 30px;
            text-align: center;
            position: relative;
            overflow: hidden;
        }

        .booking-form-header h2 {
            font-family: 'Playfair Display', serif;
            font-size: 2.5rem;
            margin-bottom: 10px;
            color: white;
        }

        .booking-form-body {
            padding: 40px;
        }

        /* Form Styles - completely unique classes */
        .booking-form-label {
            font-family: 'Montserrat', sans-serif;
            font-weight: 600;
            color: #333;
            margin-bottom: 8px;
            display: flex;
            align-items: center;
        }

        .booking-form-required {
            color: #d4af37;
            margin-left: 4px;
        }

        .booking-form-input,
        .booking-form-select {
            font-family: 'Montserrat', sans-serif;
            padding: 12px 15px;
            border-radius: 8px;
            border: 1px solid #ddd;
            transition: all 0.3s;
            background-color: #f8f4f0;
            width: 100%;
            box-sizing: border-box;
        }

        .booking-form-input:focus,
        .booking-form-select:focus {
            border-color: #d4af37;
            box-shadow: 0 0 0 0.25rem rgba(212, 175, 55, 0.25);
            outline: 2px solid #d4af37;
            outline-offset: 2px;
        }

        /* Custom Button - unique class */
        .booking-submit-btn {
            font-family: 'Montserrat', sans-serif;
            background-color: #d4af37;
            border: none;
            padding: 15px 30px;
            font-weight: 600;
            border-radius: 8px;
            transition: all 0.3s;
            font-size: 1.1rem;
            letter-spacing: 0.5px;
            color: white;
            text-decoration: none;
            display: block;
            width: 100%;
            cursor: pointer;
        }

        .booking-submit-btn:hover {
            background-color: #c19b2c;
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(212, 175, 55, 0.4);
            color: white;
        }

        /* Feature Section */
        .booking-feature-icon {
            font-size: 2.5rem;
            color: #d4af37;
            margin-bottom: 20px;
            transition: transform 0.3s ease;
        }

        .booking-feature-box {
            padding: 30px;
            border-radius: 10px;
            background: #ffffff;
            height: 100%;
            transition: transform 0.3s ease;
            border: 1px solid rgba(212, 175, 55, 0.1);
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.05);
        }

        .booking-feature-box:hover {
            transform: translateY(-10px);
            box-shadow: 0 15px 30px rgba(0, 0, 0, 0.1);
            border-color: rgba(212, 175, 55, 0.3);
        }

        .booking-feature-box:hover .booking-feature-icon {
            transform: scale(1.1);
        }

        .booking-why-section {
            background: #f8f4f0;
            padding: 80px 0;
        }

        .booking-section-title {
            position: relative;
            margin-bottom: 50px;
            text-align: center;
        }

        .booking-section-title h2 {
            font-family: 'Playfair Display', serif;
            font-size: 2.8rem;
            color: #333;
        }

        .booking-section-title::after {
            content: '';
            display: block;
            width: 100px;
            height: 4px;
            background: #d4af37;
            margin: 20px auto;
            border-radius: 2px;
        }

        .booking-form-subtitle {
            font-family: 'Playfair Display', serif;
            font-size: 1.3rem;
            color: #d4af37;
            margin-bottom: 20px;
            padding-bottom: 10px;
            border-bottom: 1px solid rgba(212, 175, 55, 0.3);
            display: flex;
            align-items: center;
        }

        .booking-details-box {
            background-color: rgba(232, 196, 196, 0.1);
            padding: 20px;
            border-radius: 10px;
            margin-bottom: 25px;
            border-left: 4px solid #d4af37;
            transition: transform 0.3s ease;
        }

        .booking-details-box:hover {
            transform: translateX(5px);
        }

        .booking-success-alert {
            display: none;
            background-color: rgba(40, 167, 69, 0.1);
            border-left: 4px solid #28a745;
            padding: 20px;
            margin-bottom: 30px;
            border-radius: 5px;
        }

        .booking-success-alert i {
            color: #28a745;
            font-size: 1.5rem;
            margin-right: 15px;
        }

        .booking-spinner {
            display: none;
            width: 1.5rem;
            height: 1.5rem;
            margin-right: 10px;
            border: 0.2em solid currentColor;
            border-right-color: transparent;
            border-radius: 50%;
            animation: booking-spin 0.75s linear infinite;
        }

        @keyframes booking-spin {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }

        /* Responsive styles */
        @media (max-width: 992px) {
            .booking-hero-title {
                font-size: 3rem;
            }
            
            .booking-hero-text {
                font-size: 1.2rem;
            }
        }

        @media (max-width: 768px) {
            .booking-hero-section {
                height: 60vh;
            }
            
            .booking-form-container {
                margin-top: -50px;
            }
            
            .booking-hero-title {
                font-size: 2.5rem;
            }
            
            .booking-section-title h2 {
                font-size: 2.2rem;
            }

            .booking-form-body {
                padding: 25px;
            }
        }

        @media (max-width: 576px) {
            .booking-hero-section {
                height: 50vh;
            }
            
            .booking-hero-title {
                font-size: 2rem;
            }
            
            .booking-form-header h2 {
                font-size: 2rem;
            }

            .booking-form-subtitle {
                font-size: 1.1rem;
            }
        }

        /* Helper classes for layout - no Bootstrap conflict */
        .booking-grid {
            display: grid;
            gap: 1rem;
        }

        .booking-grid-2 {
            grid-template-columns: repeat(2, 1fr);
        }

        .booking-grid-3 {
            grid-template-columns: repeat(3, 1fr);
        }

        .booking-flex-center {
            display: flex;
            justify-content: center;
            align-items: center;
        }

        .booking-text-center {
            text-align: center;
        }

        .booking-mb-4 {
            margin-bottom: 1.5rem;
        }

        .booking-mb-5 {
            margin-bottom: 3rem;
        }
    </style>
</head>
<body>
    <%- include('../allpack/layout/navbar') %>
    
    <!-- Booking page content with unique, non-conflicting classes -->
    <div class="booking-page-content">
        <!-- Hero Section -->
        <section class="booking-hero-section">
            <video class="booking-hero-video" autoplay loop muted playsinline>
                <source src="/assethome/booking.mp4" type="video/mp4">
                Your browser does not support the video tag.
            </video>
            <div class="booking-hero-overlay"></div>
            <div class="booking-hero-content">
                <div class="container">
                    <h1 class="booking-hero-title">Your Dream Wedding Starts Here</h1>
                    <p class="booking-hero-text">Let us craft the perfect celebration of your love story with personalized planning and exquisite attention to detail</p>
                    <a href="#booking-form" class="booking-submit-btn" style="display: inline-block; width: auto;">
                        <i class="fas fa-calendar-plus me-2"></i> Book Your Consultation
                    </a>
                </div>
            </div>
        </section>

        <!-- Booking Form Section -->
        <div class="booking-form-container" id="booking-form">
            <div class="booking-form-card">
                <div class="booking-form-header">
                    <h2><i class="fas fa-heart me-2"></i> Wedding Planning Consultation</h2>
                    <p class="mb-0">Begin your journey to the perfect wedding day</p>
                </div>
                <div class="booking-form-body">
                    <!-- Success Message -->
                    <div class="booking-success-alert booking-flex-center" id="bookingSuccessAlert">
                        <i class="fas fa-check-circle"></i>
                        <div>
                            <h5>Thank you for your request!</h5>
                            <p class="mb-0">We've received your wedding consultation request and will contact you within 24 hours.</p>
                        </div>
                    </div>
                    
                    <form id="weddingBookingForm" action="/booking/submit" method="POST">
                        <!-- Couple's Information -->
                        <div class="booking-details-box">
                          <h5 class="booking-form-subtitle"><i class="fas fa-user-friends me-2"></i> Couple's Information</h5>
                          <div class="booking-grid booking-grid-2">
                            <div class="booking-mb-4">
                              <label for="brideName" class="booking-form-label">First Name <span class="booking-form-required">*</span></label>
                              <input type="text" class="booking-form-input" id="brideName" name="couple[firstname]" required>
                            </div>
                            <div class="booking-mb-4">
                              <label for="groomName" class="booking-form-label">Last Name <span class="booking-form-required">*</span></label>
                              <input type="text" class="booking-form-input" id="groomName" name="couple[lastname]" required>
                            </div>
                          </div>
                          <div class="booking-grid booking-grid-2">
                            <div class="booking-mb-4">
                              <label for="email" class="booking-form-label">Email Address <span class="booking-form-required">*</span></label>
                              <input type="email" class="booking-form-input" id="email" name="couple[email]" required placeholder="couple@example.com">
                            </div>
                            <div class="booking-mb-4">
                              <label for="phone" class="booking-form-label">Phone Number</label>
                              <input type="tel" class="booking-form-input" id="phone" name="couple[phone]" required placeholder="+250 785300707">
                            </div>
                          </div>
                        </div>
                      
                        <!-- Wedding Details -->
                        <div class="booking-details-box">
                          <h5 class="booking-form-subtitle"><i class="fas fa-calendar-heart me-2"></i> Wedding Details</h5>
                          <div class="booking-grid booking-grid-2">
                            <div class="booking-mb-4">
                              <label for="weddingDate" class="booking-form-label">Wedding Date <span class="booking-form-required">*</span></label>
                              <input type="date" class="booking-form-input" id="weddingDate" name="weddingDate" required>
                              <small style="color: #6c757d; display: block; margin-top: 5px;">Please select a date at least 3 months from today</small>
                            </div>
                            <div class="booking-mb-4">
                              <label for="guestsNumber" class="booking-form-label">Estimated Number of Guests <span class="booking-form-required">*</span></label>
                              <input type="number" class="booking-form-input" id="guestsNumber" name="guestsNumber" required min="1" placeholder="150">
                            </div>
                          </div>
                          <div class="booking-grid booking-grid-2">
                            <div class="booking-mb-4">
                              <label for="weddingType" class="booking-form-label">Wedding Style</label>
                              <select class="booking-form-select" id="weddingType" name="weddingType" required>
                                <option value="" disabled selected>Select Wedding Style</option>
                                <option value="Gusaba no Gukwa">Gusaba no Gukwa</option>
                                <option value="Reception">Reception</option>
                                <option value="Other">Other</option>
                              </select>
                            </div>
                            <div class="booking-mb-4">
                              <label for="weddingLocation" class="booking-form-label">Wedding Location (if known)</label>
                              <input type="text" class="booking-form-input" id="weddingLocation" name="weddingLocation" placeholder="Venue name or city">
                            </div>
                          </div>
                        </div>
                      
                        <!-- Additional Information -->
                        <div class="booking-mb-5">
                          <label for="weddingVision" class="booking-form-label">Tell Us About Your Wedding Vision</label>
                          <textarea class="booking-form-input" id="weddingVision" name="weddingVision" rows="4" placeholder="Describe your dream wedding theme, colors, special requirements..."></textarea>
                        </div>
                      
                        <button type="submit" class="booking-submit-btn" id="bookingSubmitBtn">
                            <span class="booking-spinner" role="status" aria-hidden="true"></span>
                            <i class="fas fa-paper-plane me-2"></i> Request Consultation
                        </button>
                      </form>
                </div>
            </div>
        </div>

        <!-- Why Choose Us Section -->
        <section class="booking-why-section">
            <div class="container">
                <div class="booking-section-title">
                    <h2>Why Couples Choose Us</h2>
                </div>
                <div class="booking-grid booking-grid-3" style="gap: 2rem;">
                    <div class="booking-feature-box booking-text-center">
                        <div class="booking-feature-icon">
                            <i class="fas fa-gem"></i>
                        </div>
                        <h4>Luxury Experience</h4>
                        <p>We specialize in creating luxurious, stress-free wedding experiences with attention to every detail.</p>
                    </div>
                    <div class="booking-feature-box booking-text-center">
                        <div class="booking-feature-icon">
                            <i class="fas fa-hand-holding-heart"></i>
                        </div>
                        <h4>Personalized Service</h4>
                        <p>Your wedding is unique, and we tailor our services to reflect your personal love story.</p>
                    </div>
                    <div class="booking-feature-box booking-text-center">
                        <div class="booking-feature-icon">
                            <i class="fas fa-network-wired"></i>
                        </div>
                        <h4>Vendor Network</h4>
                        <p>Access to our exclusive network of top-tier wedding vendors and venues.</p>
                    </div>
                </div>
            </div>
        </section>
    </div> <!-- End of booking-page-content -->
    
    <%- include('../allpack/layout/footer') %>
    
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            // Form elements
            const form = document.getElementById('weddingBookingForm');
            const successMessage = document.getElementById('bookingSuccessAlert');
            const submitButton = document.getElementById('bookingSubmitBtn');
            
            if (!form || !successMessage || !submitButton) {
                console.error('Booking form elements not found');
                return;
            }
            
            // Set minimum date to today
            const today = new Date().toISOString().split('T')[0];
            const weddingDateInput = document.getElementById('weddingDate');
            if (weddingDateInput) {
                weddingDateInput.min = today;
                
                // Date validation
                weddingDateInput.addEventListener('change', function() {
                    const selectedDate = new Date(this.value);
                    const minDate = new Date();
                    minDate.setMonth(minDate.getMonth() + 3);
                    
                    if (selectedDate < minDate) {
                        alert('Please select a date at least 3 months from today to allow proper planning time.');
                        this.value = '';
                    }
                });
            }
            
            // Form submission
            form.addEventListener('submit', async function(e) {
                e.preventDefault();
                
                // Show loading state
                const spinner = submitButton.querySelector('.booking-spinner');
                const submitIcon = submitButton.querySelector('.fa-paper-plane');
                if (spinner) spinner.style.display = 'inline-block';
                if (submitIcon) submitIcon.style.display = 'none';
                submitButton.disabled = true;
                
                try {
                    // Collect form data
                    const formData = {
                        firstname: this.elements['couple[firstname]'].value,
                        lastname: this.elements['couple[lastname]'].value,
                        email: this.elements['couple[email]'].value,
                        phone: this.elements['couple[phone]'].value,
                        weddingDate: this.elements.weddingDate.value,
                        guestsNumber: this.elements.guestsNumber.value,
                        weddingType: this.elements.weddingType.value,
                        weddingLocation: this.elements.weddingLocation.value,
                        weddingVision: this.elements.weddingVision.value
                    };
        
                    // Send to server
                    const response = await fetch('/booking/submit', {
                        method: 'POST',
                        headers: {
                            'Content-Type': 'application/json',
                        },
                        body: JSON.stringify(formData)
                    });
                    
                    const result = await response.json();
                    
                    if (response.ok) {
                        // Show success message
                        form.style.display = 'none';
                        successMessage.style.display = 'flex';
                        successMessage.scrollIntoView({ behavior: 'smooth' });
                        
                        // Reset form after delay
                        setTimeout(() => {
                            form.reset();
                            form.style.display = 'block';
                            successMessage.style.display = 'none';
                        }, 5000);
                    } else {
                        alert('Error: ' + (result.message || 'Failed to submit form'));
                    }
                } catch (error) {
                    console.error('Error:', error);
                    alert('An error occurred while submitting the form');
                } finally {
                    // Reset button state
                    if (spinner) spinner.style.display = 'none';
                    if (submitIcon) submitIcon.style.display = 'inline-block';
                    submitButton.disabled = false;
                }
            });
            
            // Smooth scrolling
            document.querySelectorAll('a[href^="#"]').forEach(anchor => {
                anchor.addEventListener('click', function(e) {
                    e.preventDefault();
                    
                    const targetId = this.getAttribute('href');
                    if (targetId === '#') return;
                    
                    const targetElement = document.querySelector(targetId);
                    if (targetElement) {
                        window.scrollTo({
                            top: targetElement.offsetTop - 100,
                            behavior: 'smooth'
                        });
                    }
                });
            });
        });
    </script>
</body>
</html>