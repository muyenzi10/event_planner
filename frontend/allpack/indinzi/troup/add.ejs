<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Decor | Dream Day Events</title>

  <!-- Bootstrap & Icons -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">
  <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@600&family=Poppins:wght@400;500;600&display=swap" rel="stylesheet">

  <style>
    :root {
      --primary-color: #d4af37;
      --accent-color: #e91e63;
      --bg-color: #f8f4f0;
    }

    body {
      font-family: 'Poppins', sans-serif;
      background-color: var(--bg-color);
      margin: 0;
      height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
    }

    .card {
      border-radius: 20px;
      box-shadow: 0 10px 30px rgba(0,0,0,0.06);
      padding: 2rem;
      width: 100%;
      max-width: 600px;
    }

    .card h2 {
      font-family: 'Playfair Display', serif;
      color: var(--primary-color);
      text-align: center;
      margin-bottom: 2rem;
    }

    label.required:after { content:" *"; color: var(--accent-color); }

    .btn-save {
      background: linear-gradient(90deg,var(--accent-color),#ff9800);
      color: #fff;
      border: none;
      width: 100%;
    }

    .btn-delete {
      background-color: #dc3545;
      color: #fff;
      border: none;
      width: 100%;
    }

    .preview-media img,
    .preview-media video {
      max-width: 100%;
      max-height: 180px;
      border-radius: 12px;
      border: 1px solid #ddd;
      display: block;
      margin: auto;
    }

    @media (max-width: 480px) {
      .card {
        padding: 1.5rem;
      }
      .preview-media img, .preview-media video {
        max-height: 140px;
      }
    }
  </style>
</head>
<body>

<div class="card">
  <h2>Upload Troupe File</h2>


  <form action="/upload/troupe/image" method="POST" enctype="multipart/form-data">
    <div class="mb-3">
      <label class="form-label required">Title</label>
      <input type="text" name="title"  class="form-control">
    </div>

    <div class="mb-3">
      <label class="form-label ">Description</label>
      <textarea name="description" rows="3" class="form-control"></textarea>
    </div>

    <div class="mb-3">
      <label class="form-label">Replace file (optional)</label>
      <input type="file" name="media" accept="image/*,video/*" class="form-control">
      
    </div>
    <div class="d-grid gap-2">
      <button type="submit" class="btn btn-save btn-lg">Save Changes</button>
    <a href="#" class="btn btn-save btn-lg">Go Back</a>

    </div>
  </form>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
